apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: openproject
spec:
  selector:
    app: postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  clusterIP: None  # headless — StatefulSet dùng DNS trực tiếp
